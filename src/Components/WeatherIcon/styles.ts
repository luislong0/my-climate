import { styled } from "../../styles";

export const IconContainer = styled('div', {
  svg: {
    width: '3.75rem',
    height: '3.75rem',

    backgroundColor: '$violet500',
    padding: '0.625rem',
    borderRadius: 8,
  }
})